copy vid.hh to <click src root>/include/click

replace confparse.cc under lib and confparse.hh under include/click


to build veil elements only:

1. place the 'veil' directory directly under <click src root>/elements or <click src root>/elements/local

2. copy configure and Makefile.in to 'veil'

2. run configure with appropriate options
	./configure <other options> --prefix=<click install dir>
use --enable-local if 'veil' was placed under 'local' in step 1.
click install dir is typically /usr/local

3. run
	make elemlist

4. run
	make install



